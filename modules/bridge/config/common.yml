imports:
  - { resource: partials/api.yml }
  - { resource: partials/service.yml }

services:
  bridge.module:
    public: true
    class: 'Bridge'
    factory: ['Module', 'getInstanceByName']
    arguments:
      - 'bridge'