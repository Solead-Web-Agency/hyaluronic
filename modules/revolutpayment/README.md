# README

## Revolut Gateway for PrestaShop
### Local PrestaShop Setup

- Navigate `revolutpayment` (root) folder
- Install [docker-compose](https://www.docker.com/)
- Install [composer](https://getcomposer.org/)
- Run the local environment (docker containers) & Unit tests with:

```sh
composer docker:up
```
